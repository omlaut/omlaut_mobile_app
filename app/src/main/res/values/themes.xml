<resources>
    <!-- Base application theme. -->
    <style name="Theme.Omlaut_mobile_app" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/omlaut_green</item>
        <item name="colorPrimaryDark">@color/omlaut_green</item>
        <item name="colorAccent">@color/teal_200</item>
        <!-- Add other customization items here -->
    </style>
    <style name="ErrorEditTextStyle">
        <item name="android:background">@drawable/edit_text_error_background</item>
        <item name="android:textColor">#FF0000</item>
    </style>
    <style name="NavigationViewStyle" parent="Widget.Design.NavigationView">
        <item name="itemTextColor">@color/nav_item_text</item>
        <item name="itemBackground">@color/nav_item_background</item>
    </style>
    <style name="NavigationViewTheme" parent="Theme.AppCompat">
        <item name="colorControlHighlight">@color/nav_item_background_selected</item>
        <item name="android:textColorPrimary">@color/black</item>
    </style>
    <style name="NavItemTextAppearance" parent="TextAppearance.AppCompat.Body1">
        <item name="android:textColor">@color/black</item>
    </style>
</resources>
